{
  "name": "mi-sales",
  "version": "1.0.0",
  "scripts": {
    "build": "gatsby build",
    "deploy": "gatsby build && gh-pages -d public -b gh-pages"
  },
  "dependencies": {
    "gatsby": "^5.0.0"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
